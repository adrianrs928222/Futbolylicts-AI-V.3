# API y caché

API-Football: fixtures, forma y clasificación. The Odds API: cuotas. Supabase: caché persistente y contadores. Los TTL y presupuestos están en `lib/config/rules.ts` y pueden sobrescribirse por variables de entorno.
